apiVersion: v1
kind: Service
metadata:
  name: sentinelauth-service
spec:
  type: NodePort
  selector:
    app: sentinelauth-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007