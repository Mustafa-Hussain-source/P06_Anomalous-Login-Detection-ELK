apiVersion: v1
kind: Service
metadata:
  name: sentinelauth-backend-service
spec:
  type: NodePort
  selector:
    app: sentinelauth-backend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30008