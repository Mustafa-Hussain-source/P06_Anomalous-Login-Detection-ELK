name: ci-cd

on:
  push:
    branches: ["main", "master", "dev"]
  pull_request:
    branches: ["main", "master", "dev"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "20"
  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  BACKEND_IMAGE_REPO: p06-anomalous-login-detection-elk-backend

jobs:
  python-checks:
    name: Backend Checks + Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: Development/Sprint-3/requirements.txt

      - name: Install Python dependencies
        working-directory: Development/Sprint-3
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install coverage pytest

      - name: Syntax check Sprint-3 backend
        working-directory: Development/Sprint-3
        run: python -m compileall app

      - name: Syntax check Sprint-4 automation
        working-directory: Development/Sprint-4
        run: python -m compileall uc_automation.py test_uc_automation.py

      - name: Run Sprint-4 tests with coverage
        working-directory: Development/Sprint-4
        run: |
          coverage run -m unittest test_uc_automation.py
          coverage xml -o coverage.xml

      - name: Dependency check
        working-directory: Development/Sprint-3
        run: python -m pip check

      - name: SonarCloud Scan
        if: ${{ env.SONAR_TOKEN != '' && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false) }}
        uses: SonarSource/sonarqube-scan-action@v6
        with:
          args: >
            -Dsonar.python.coverage.reportPaths=Development/Sprint-4/coverage.xml
        env:
          SONAR_HOST_URL: https://sonarcloud.io
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  frontend-checks:
    name: Frontend Lint + Build + Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Development/Sprint-4/Front-End/sentinelauth-ui

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: Development/Sprint-4/Front-End/sentinelauth-ui/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Lint frontend
        run: npm run lint

      - name: Run frontend tests
        run: npm run test:run -- --passWithNoTests

      - name: Build frontend
        run: npm run build

  docker-build:
    name: Backend Docker Build
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' }}
    needs:
      - python-checks
      - frontend-checks

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Sprint-3 backend image
        run: |
          docker build \
            -f Development/Sprint-3/Dockerfile \
            -t alds-sprint3-api:${{ github.sha }} \
            Development/Sprint-3

  docker-publish:
    name: Backend Docker Publish
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    needs:
      - python-checks
      - frontend-checks
    outputs:
      image_name: ${{ steps.image.outputs.name }}
      image_ref: ${{ steps.image.outputs.ref }}
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compute image coordinates
        id: image
        run: |
          owner_lc=$(printf '%s' "$GITHUB_REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')
          image_name="ghcr.io/${owner_lc}/${BACKEND_IMAGE_REPO}"
          echo "name=${image_name}" >> "$GITHUB_OUTPUT"
          echo "ref=${image_name}:sha-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-,format=long
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Sprint-3 backend image
        uses: docker/build-push-action@v6
        with:
          context: Development/Sprint-3
          file: Development/Sprint-3/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-dev-render:
    name: Deploy Dev Render Preview
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    needs:
      - docker-publish
    env:
      IMAGE_REF: ${{ needs.docker-publish.outputs.image_ref }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Render dev deployment manifest
        run: |
          mkdir -p rendered/dev
          cat Development/Sprint-4/k8s/namespace.yaml > rendered/dev/dev-rendered.yaml
          printf "\n---\n" >> rendered/dev/dev-rendered.yaml
          cat Development/Sprint-4/k8s/configmap.yaml >> rendered/dev/dev-rendered.yaml
          printf "\n---\n" >> rendered/dev/dev-rendered.yaml
          sed "s|image: alds-sprint3-api:v1.0|image: ${IMAGE_REF}|" Development/Sprint-4/k8s/deployment.yaml \
            >> rendered/dev/dev-rendered.yaml
          printf "\n---\n" >> rendered/dev/dev-rendered.yaml
          cat Development/Sprint-4/k8s/service.yaml >> rendered/dev/dev-rendered.yaml

      - name: Validate rendered manifest
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workdir" \
            ghcr.io/yannh/kubeconform:latest \
            -strict -summary /workdir/rendered/dev/dev-rendered.yaml

      - name: Upload rendered dev manifest
        uses: actions/upload-artifact@v4
        with:
          name: dev-rendered-manifest
          path: rendered/dev/dev-rendered.yaml

  deploy-dev:
    name: Deploy Dev Smoke Validation
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    needs:
      - docker-publish
      - deploy-dev-render
    environment:
      name: dev
    env:
      IMAGE_REF: ${{ needs.docker-publish.outputs.image_ref }}
    permissions:
      contents: read
      packages: read

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull backend image
        run: docker pull "$IMAGE_REF"

      - name: Start dev validation container
        run: |
          docker run -d --rm --name alds-dev-smoke -p 18000:8000 \
            "$IMAGE_REF"

      - name: Wait for app readiness
        run: |
          for attempt in {1..20}; do
            if curl --fail --silent http://127.0.0.1:18000/docs > /dev/null; then
              exit 0
            fi
            sleep 3
          done
          echo "Application did not become ready in time" >&2
          exit 1

      - name: Smoke check docs endpoint
        run: curl --fail http://127.0.0.1:18000/docs

      - name: Smoke check traffic status endpoint
        run: curl --fail http://127.0.0.1:18000/traffic/status

      - name: Dump container logs on failure
        if: ${{ failure() }}
        run: |
          if docker inspect alds-dev-smoke > /dev/null 2>&1; then
            docker logs alds-dev-smoke
          else
            echo "Container alds-dev-smoke was not created"
          fi

      - name: Stop dev validation container
        if: ${{ always() }}
        run: |
          if docker inspect alds-dev-smoke > /dev/null 2>&1; then
            docker stop alds-dev-smoke
          else
            echo "Container alds-dev-smoke is not running"
          fi
